/**
 * Enums for Discord MCP Server
 */

export enum ResponseFormat {
  MARKDOWN = 'markdown',
  JSON = 'json',
}

export enum AnnouncementStyle {
  RELEASE = 'release',
  HOTFIX = 'hotfix',
  BETA = 'beta',
  CUSTOM = 'custom',
}
